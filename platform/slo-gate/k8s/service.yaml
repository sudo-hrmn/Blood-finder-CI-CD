apiVersion: v1
kind: Service
metadata:
  name: slo-gate
  namespace: slo-gate
  labels:
    app: slo-gate
spec:
  selector:
    app: slo-gate
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
